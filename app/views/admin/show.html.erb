<h1>Admin Dashboard</h1>
<br>
<div class="col-sm-4" id="admin-buttons">
  <p><%= link_to "Create New Menu Item", new_menu_item_path(@item), class: "btn btn-success" %></p>
</div>

<div class="col-sm-4" id="admin-buttons">
  <p><%= link_to "Create New Category", new_menu_category_path(@item), class: "btn btn-success" %></p>
</div>
<div class="col-sm-4" id="admin-buttons">
  <p><%= link_to "See All Items", menu_path, class: "btn btn-success" %></p>
</div>
<br>
<br>
<h3> Orders by Status:</h3>

<ul class="nav nav-tabs" id="myTab">
  <li class="active"><a href="#all-orders">All Orders <span class="badge"><%= @orders.count %></span></a></li>
  <li><a href="#ordered">Ordered <span class="badge"><%= @orders.ordered.count %></span></a></li>
  <li><a href="#paid">Paid <span class="badge"><%= @orders.paid.count %></span></a></li>
  <li><a href="#cancelled">Cancelled <span class="badge"><%= @orders.cancelled.count %></span></a></li>
  <li><a href="#completed">Completed <span class="badge"><%= @orders.completed.count %></span></a></li>
</ul>

<div id='content' class="tab-content">
  <div class="tab-pane active" id="all-orders">
    <table class="table">
      <tr>
        <th>Order ID</th>
        <th>Customer</th>
        <th>Date Ordered</th>
        <th>Status</th>
      </tr>
      <% @orders.each do |order| %>
      <tr>
        <td><%= link_to "Order #{order.id}", order_path(order) %></td>
        <td><%= link_to "#{order.user.full_name}", account_path(order.user) %>
        <%=  %></td>
        <td><%= format_time(order.created_at) %></td>
        <td><%= order.status %></td>
      </tr>
      <% end %>
    </table>
  </div>

  <div class="tab-pane" id="ordered">
    <table class="table">
      <tr>
        <th>Order ID</th>
        <th>Customer</th>
        <th>Date Ordered</th>
        <th>Status</th>
        <th>Action</th>
      </tr>
      <% @orders.ordered.each do |order| %>
      <tr>
        <td><%= link_to "Order #{order.id}", order_path(order) %></td>
        <td><%= link_to "#{order.user.full_name}", account_path(order.user) %>
        <%=  %></td>
        <td><%= format_time(order.created_at) %></td>
        <td><%= order.status %></td>
        <td><%= link_to "Cancel Order", cancel_order_path(order), class: "btn btn-xs btn-danger" %>
            <%= link_to "Mark as Paid", paid_order_path(order), class: "btn btn-xs btn-info" %></td>
      </tr>
      <% end %>
    </table>
  </div>

  <div class="tab-pane" id="paid">
    <table class="table">
      <tr>
        <th>Order ID</th>
        <th>Customer</th>
        <th>Date Ordered</th>
        <th>Status</th>
        <th>Actions</th>
      </tr>
      <% @orders.paid.each do |order| %>
      <tr>
        <td><%= link_to "Order #{order.id}", order_path(order) %></td>
        <td><%= link_to "#{order.user.full_name}", account_path(order.user) %>
        <%=  %></td>
        <td><%= format_time(order.created_at) %></td>
        <td><%= order.status %></td>
        <td><%= link_to "Cancel Order", cancel_order_path(order), class: "btn btn-xs btn-danger" %>
            <%= link_to "Mark as Completed", complete_order_path(order), class: "btn btn-xs btn-primary" %></td>
      </tr>
      <% end %>
    </table>
  </div>

  <div class="tab-pane" id="cancelled">
    <table class="table">
      <tr>
        <th>Order ID</th>
        <th>Customer</th>
        <th>Date Ordered</th>
        <th>Date Cancelled</th>
        <th>Status</th>
      </tr>
      <% @orders.cancelled.each do |order| %>
      <tr>
        <td><%= link_to "Order #{order.id}", order_path(order) %></td>
        <td><%= link_to "#{order.user.full_name}", account_path(order.user) %>
        <%=  %></td>
        <td><%= format_time(order.created_at) %></td>
        <td><%= format_time(order.updated_at) %></td>
        <td><%= order.status %></td>
      </tr>
      <% end %>
    </table>
  </div>

  <div class="tab-pane" id="completed">
    <table class="table">
      <tr>
        <th>Order ID</th>
        <th>Customer</th>
        <th>Date Ordered</th>
        <th>Date Completed</th>
        <th>Status</th>
      </tr>
      <% @orders.completed.each do |order| %>
      <tr>
        <td><%= link_to "Order #{order.id}", order_path(order) %></td>
        <td><%= link_to "#{order.user.full_name}", account_path(order.user) %>
        <%=  %></td>
        <td><%= format_time(order.created_at) %></td>
        <td><%= format_time(order.updated_at) %></td>
        <td><%= order.status %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>

<script>
  $('#myTab a').click(function (e) {
    e.preventDefault()
    $(this).tab('show')
  })
</script
